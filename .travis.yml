language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t gitznik/docker-react -f Dockerfile.dev .


script:
  - docker run -e CI=true gitznik/docker-react npm run test
